# Examples boundary

No executable example is published in Checkpoint 2 because there is no MCP runtime yet. Examples begin
only after the real protocol surface exists. They must call registered Tools through MCP and must never
fall back to direct browser automation or the frozen upper-layer prototype.
